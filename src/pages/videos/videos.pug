extends ../page-template/page-template.pug
include ../../blocks/video/video.pug
include ../../blocks/dropdown/dropdown.pug


block page-title
    title Videos

block page-content
    main.videos
        include __header/videos__header.pug
        - const orderOptions = ["by default", "by popularity", "newest first", "oldest first"];
        +dropdown({options: orderOptions, hasDefault: true, mixClass: "videos__order"})
        include __content/videos__content.pug