include ../calendar/calendar.pug
include ../page-section/page-section.pug

mixin calendar-section(mixClass="")
    - mixClass = mixClass ? " " + mixClass : "";
    +page-section("Calendar", "calendar-section" + mixClass)
        +calendar()